# 异步教程 —— 上下文管理、协程与线程

五讲,回答三个问题:**with 到底在干什么、async/await 的并发是怎么运转的、多线程在企业里还有没有活路**。

配套关系:本教程和[锁教程](../锁教程/README.md)是一对——锁教程管"数据不被写坏",本教程管"代码怎么同时干多件事"。第 4 讲里线程丢数的实验,和锁教程第 1 讲数据库丢更新是同一个病。

**仓库里不带你抄的现成 .py**——每个脚本的全文代码只存在于讲义里,照着敲出来、存到对应目录,才是这套教程的用法:

```
读开场图(这讲解决什么问题) → 对着讲义敲代码(建议手敲,别复制;
跑脚本、做实验(讲义"跑起来"一节给了原样可复制的命令和预期输出)
→ 回答过关自测(答不上再回头翻)
```

## 学习地图

```
 01 上下文管理器(资源的安全边界)
        │  —— 一切资源管理的地基,FastAPI/SQLAlchemy 全靠它
        ▼
 02 协程与事件循环(单线程的并发)
        │  —— await 是让位点;time.sleep 是全场冻结
        ▼
 03 并发编排(gather / TaskGroup / Semaphore / timeout)
        │  —— 并发也要管束:限流、超时、谁先回收谁
        ▼
 04 线程、进程与阻塞桥接(GIL、to_thread、进程池)
        │  —— 企业真话:线程用在哪、不用在哪
        ▼
 05 企业场景(从 FastAPI 到 Celery)
           —— 把 01-04 拼成一个迷你转账 API,工作项目的骨架全图
```

| 讲 | 主题 | 学完你会 | 涉及实验 |
|---|---|---|---|
| 01 | [上下文管理器](01-上下文管理器/讲义.md) | with/async with 协议、contextmanager、事务自动回滚 | 中途炸了连接照样关;手搓工作项目同款 get_db_session |
| 02 | [协程与事件循环](02-协程与事件循环/讲义.md) | 协程对象、await 让位、Task 并发 | 串行 3 秒 vs 并发 1 秒;time.sleep 冻结全场实录 |
| 03 | [并发编排](03-并发编排/讲义.md) | gather、TaskGroup、Semaphore、timeout、as_completed | 20 请求过 5 窗口;超时掐表;谁快先收谁 |
| 04 | [线程与进程](04-线程与进程/讲义.md) | GIL、threading.Lock、to_thread、进程池、企业真实用法 | 线程丢数 2993 次;CPU 任务三池对比;心跳冻结 vs 不冻 |
| 05 | [企业场景](05-企业场景/讲义.md) | lifespan、yield 依赖、阻塞路由的病、选型 | 迷你转账 API:并发对转,锁版分毫不差,无锁版丢一笔 |

## 先回答你最关心的:企业里真的用多线程吗

**直接结论(第 4 讲展开):**

| 场景 | 企业的选择 |
|---|---|
| Web 服务(IO 密集,新代码) | **asyncio + FastAPI**(工作项目就是) |
| Web 服务里遇到只有同步版的 SDK | `asyncio.to_thread` 桥接,**不另起线程体系** |
| CPU 密集(转码、压缩、报表) | **独立进程**:Celery worker(工作项目就是)/ 容器拆分,不用线程池硬扛 |
| 简单脚本并发下载/调接口 | `ThreadPoolExecutor` 十行搞定,够用 |
| 老项目、特定 SDK(JDBC 风格驱动、NumPy 生态) | 线程仍在服役 |

Python 的多线程因为 GIL,同一时刻只有一个线程执行 Python 字节码——**线程只对"等 IO"有收益,对"算数"没有**。所以服务端新代码主线是 asyncio,线程退居"桥接阻塞调用"的配角,重 CPU 一律甩给进程。这不是"用不用"的问题,是"用在哪一层"的问题。

## 环境

| 项 | 说明 |
|---|---|
| 依赖 | 仓库 `uv` 环境已备齐,纯 asyncio 实验零依赖;第 1、5 讲用公网 PostgreSQL(`.env` 的 `DATABASE_URL`) |
| 跑法 | 一律在仓库根目录 `uv run python "异步教程/NN-xxx/xxx.py"` |
| 第 5 讲 | 起 API 服务用 8010 端口(8000 常年被占用),用 curl 做并发实验 |
| 清场 | 表 `async01_user`、`async05_account`,不用管,脚本开场自重置 |

## 学完标准

- 看到 `async with session:` 能说出**谁在什么时候 commit/rollback/close**
- 看到 `await` 能指出**让位点**,看到 `time.sleep` 在协程里能立刻报警
- 能默写"IO 密集 → asyncio;阻塞库 → to_thread;CPU 密集 → 进程/Celery"三行选型
- 知道**事件循环里唯一的线程被谁占着,谁就是全服务的瓶颈**
