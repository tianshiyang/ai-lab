# 锁概念地图

六讲的武器收进一张图:**遇到什么威胁,拔哪把刀**。图用 Mermaid 画,GitHub / Typora / Obsidian 里直接渲染;看不了图就看下面的表,信息不缺。

## 1 选型决策树

```mermaid
flowchart TD
    Q["并发要保护什么?"]
    Q --> A["单行数字增减 / 状态置位"]
    Q --> B["复杂读-改-写"]
    Q --> C["同一业务身份只许一份"]
    Q --> D["多 worker 抢任务"]
    Q --> E["跨进程互斥"]

    A --> A1["一条 SQL:UPDATE SET x = x±n<br/>WHERE x >= n(第 1、3 讲)"]

    B --> B1{"冲突频率?"}
    B1 -->|少| B2["乐观锁:version CAS + 重试(第 3 讲)"]
    B1 -->|多/热点| B3["悲观锁:FOR UPDATE 短事务(第 2 讲)"]

    C --> C1["唯一约束 + ON CONFLICT(第 4 讲)<br/>幂等键 = 业务身份"]

    D --> D1["FOR UPDATE SKIP LOCKED(第 2 讲)"]

    E --> E1{"互斥强度?"}
    E1 -->|尽力即可| E2["Redis 锁:NX+PX+token+Lua(第 5 讲)"]
    E1 -->|严格| E3["数据库行锁 / ZooKeeper / etcd"]
```

## 2 Redis 锁的完整生命周期

```mermaid
sequenceDiagram
    participant A as 持锁者
    participant R as Redis
    participant B as 竞争者
    A->>R: SET lock token NX PX 30000 ✅
    B->>R: SET lock token NX PX 30000 ❌(NX 撞了)
    Note over A,R: 看门狗每 px/3 续期(第 5 讲)
    A->>R: EVAL"get==token 则 del"(Lua 原子)
    B->>R: SET lock token NX PX 30000 ✅(A 释放后)
```

三句口诀:

- **一条 SQL 能解决的,不上锁**;数据库锁能解决的,不上分布式锁;单机锁能解决的,不上 Redis
- **悲观锁的命门是持锁时长**(事务要短),乐观锁的命门是冲突率(高了重试风暴),Redis 锁的命门是过期时间(短了互斥失效,长了崩溃死锁)
- **唯一约束永远在场**——它是唯一不依赖应用代码正确性的防线

## 3 易混点对照表

| 对比项 | 悲观锁 | 乐观锁 | 唯一约束 | Redis 锁 |
|---|---|---|---|---|
| 保护对象 | 改同一行 | 改同一行 | 插同一身份 | 进程外任意临界区 |
| 真的加锁了吗 | 是(数据库行锁) | 否(条件 UPDATE) | 是(唯一索引) | 是(key 互斥) |
| 冲突方感受 | 排队/NOWAIT 报错 | rowcount=0 重试 | 23505/1062 报错 | SET 失败/等待 |
| 死锁可能 | 有(固定顺序防) | 无 | 无 | 无(但有"过期重叠") |
| 崩溃后果 | 事务回滚自动放锁 | 无状态,无影响 | 无影响 | 靠过期时间自愈 |
| 一句话 | 先锁后用 | 先干再验 | 一山不容二虎 | 进程外的门闩 |
